import { Box } from '@mui/system'
import React from 'react'

const RadioSection = ({value, handleChange}) => {
  return (
    <Box>
      {value}
    </Box>
  )
}

export default RadioSection